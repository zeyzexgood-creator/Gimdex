name: mynest
description: "A new Flutter project created with DevStudio."
publish_to: 'none'

version: 1.0.0+1

environment:
sdk: '>=3.3.0 <4.0.0'

dependencies:
flutter:
sdk: flutter
cupertino_icons: ^1.0.6
flame: ^1.15.0
  flame_audio: ^2.1.0  # untuk efek suara

dev_dependencies:
flutter_test:
sdk: flutter
flutter_lints: ^3.0.0

flutter:
uses-material-design: true